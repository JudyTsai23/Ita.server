# datasource
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ita_database?allowMultiQuery=true
    username: ita
    password: bagel

# mybatis
mybatis:
  mapper-locations:
    - classpath:mybatis/mapper/*.xml